# 📚 مكتبة النحو والإعراب - دليل الإضافة

## ✅ ما تم إنشاؤه

### 1. **الصفحات الرئيسية**
- `/grammar/index.html` - صفحة تصفح الكتب
- `/grammar/book.html` - قارئ الكتب التفاعلي
- `/grammar/books.json` - بيانات الكتب
- `/grammar/exercises.json` - بيانات التمارين

### 2. **المميزات المتوفرة**
- ✅ تصفح الكتب حسب التصنيف (نحو، إعراب، صرف، بلاغة)
- ✅ قارئ كتب مع فهرس جانبي
- ✅ التنقل بين الفصول
- ✅ تكبير/تصغير الخط
- ✅ حفظ تقدم القراءة في localStorage
- ✅ إضافة علامات مرجعية
- ✅ تصميم responsive للموبايل

---

## 📖 كيفية إضافة كتاب جديد

### الخطوة 1: تحضير ملفات الكتاب

1. أنشئ مجلد للكتاب في:
```
/grammar/data/books/اسم-الكتاب/
```

2. قسّم الكتاب إلى فصول (ملفات HTML منفصلة):
```
/grammar/data/books/النحو-الواضح/
  ├── chapter-1.html
  ├── chapter-2.html
  ├── chapter-3.html
  └── metadata.json
```

### الخطوة 2: تنسيق ملفات الفصول

كل ملف فصل يجب أن يكون HTML بسيط:

```html
<div class="chapter-content">
    <h4>المبتدأ والخبر</h4>
    <p>المبتدأ هو الاسم المرفوع الذي نبتدئ به الكلام...</p>
    <p><strong>مثال:</strong> العلمُ نورٌ</p>
    <ul>
        <li>العلمُ: مبتدأ مرفوع</li>
        <li>نورٌ: خبر مرفوع</li>
    </ul>
</div>
```

**نصائح مهمة:**
- استخدم الـ HTML فقط (بدون CSS inline معقد)
- الأمثلة داخل `<strong>` أو `<em>`
- القواعد في قوائم `<ul>` أو `<ol>`
- العناوين الفرعية `<h4>` أو `<h5>`

### الخطوة 3: تحديث books.json

أضف معلومات الكتاب في `/grammar/books.json`:

```json
{
  "books": [
    {
      "id": "nahw-wadih",
      "title": "النحو الواضح في قواعد اللغة العربية",
      "author": "علي الجارم ومصطفى أمين",
      "category": "نحو",
      "level": "مبتدئ",
      "description": "كتاب مبسط في قواعد النحو العربي",
      "chapters": [
        {"id": 1, "title": "الجملة المفيدة", "file": "nahw-wadih/chapter-1.html"},
        {"id": 2, "title": "أقسام الكلام", "file": "nahw-wadih/chapter-2.html"},
        {"id": 3, "title": "المبتدأ والخبر", "file": "nahw-wadih/chapter-3.html"}
      ],
      "totalChapters": 50
    }
  ]
}
```

**الحقول المطلوبة:**
- `id`: معرّف فريد بالإنجليزية (مثل: `nahw-wadih`)
- `title`: اسم الكتاب بالعربية
- `author`: المؤلف
- `category`: التصنيف (`نحو` / `إعراب` / `صرف` / `بلاغة`)
- `level`: المستوى (`مبتدئ` / `متوسط` / `متقدم`)
- `chapters[]`: قائمة الفصول
  - `id`: رقم الفصل
  - `title`: عنوان الفصل
  - `file`: مسار الملف النسبي

---

## 🎯 إضافة تمارين وتدريبات

### تحديث exercises.json

```json
{
  "exercises": [
    {
      "id": "irab-basics-1",
      "title": "إعراب الكلمات - المستوى الأول",
      "topic": "إعراب",
      "difficulty": "سهل",
      "questions": [
        {
          "id": 1,
          "type": "multiple-choice",
          "question": "ما إعراب كلمة (الطالبُ) في: الطالبُ مجتهدٌ؟",
          "options": [
            "مبتدأ مرفوع",
            "فاعل مرفوع",
            "مفعول به منصوب",
            "خبر مرفوع"
          ],
          "correct": 0,
          "explanation": "الطالبُ: مبتدأ مرفوع وعلامة رفعه الضمة"
        }
      ]
    }
  ]
}
```

### أنواع الأسئلة المدعومة:
1. **multiple-choice**: اختيار من متعدد
2. **fill-blank**: املأ الفراغ
3. **true-false**: صح أم خطأ
4. **match**: مطابقة

---

## 🔄 خطوات التحويل السريع

إذا كانت لديك كتب HTML كاملة:

### 1. تقسيم الكتاب بـ Python Script:

```python
from bs4 import BeautifulSoup
import re
import json

def split_book_to_chapters(html_file):
    with open(html_file, 'r', encoding='utf-8') as f:
        soup = BeautifulSoup(f.read(), 'html.parser')
    
    chapters = soup.find_all(['h2', 'h3'])  # العناوين الرئيسية
    
    for i, chapter in enumerate(chapters, 1):
        title = chapter.get_text().strip()
        content = []
        
        # جمع المحتوى بعد العنوان
        for sibling in chapter.find_next_siblings():
            if sibling.name in ['h2', 'h3']:
                break
            content.append(str(sibling))
        
        # حفظ الفصل
        with open(f'chapter-{i}.html', 'w', encoding='utf-8') as out:
            out.write(f'<div class="chapter-content">\n')
            out.write(f'<h4>{title}</h4>\n')
            out.write('\n'.join(content))
            out.write('</div>')
        
        print(f"✅ تم حفظ: {title}")

# تشغيل
split_book_to_chapters('النحو_الواضح.html')
```

### 2. توليد metadata.json تلقائياً:

```python
chapters_data = {
    "chapters": [
        {"id": i+1, "title": f"الفصل {i+1}", "file": f"chapter-{i+1}.html"}
        for i in range(50)  # عدد الفصول
    ]
}

with open('metadata.json', 'w', encoding='utf-8') as f:
    json.dump(chapters_data, f, ensure_ascii=False, indent=2)
```

---

## 🚀 النشر والتفعيل

1. **ارفع الملفات**:
```bash
cd "/c/python apps/arabic_qamoos/frontend-deploy"
npx wrangler pages deploy . --project-name qamoos-org --branch main
```

2. **اختبر محلياً**:
```bash
cd frontend-deploy
python -m http.server 8000
# افتح: http://localhost:8000/grammar/
```

3. **الرابط المباشر**:
   - https://qamoos.org/grammar/

---

## 📊 هيكل المجلدات النهائي

```
frontend-deploy/
└── grammar/
    ├── index.html          (صفحة التصفح)
    ├── book.html           (القارئ)
    ├── practice.html       (التمارين - قريباً)
    ├── books.json          (بيانات الكتب)
    ├── exercises.json      (التمارين)
    └── data/
        └── books/
            ├── nahw-wadih/
            │   ├── chapter-1.html
            │   ├── chapter-2.html
            │   └── metadata.json
            ├── irab-quran/
            └── qatir-nada/
```

---

## 💡 أفكار للتطوير المستقبلي

1. **نظام البحث داخل الكتب**
2. **تسليط الضوء على النصوص (Highlighting)**
3. **نظام النقاط والإنجازات للتمارين**
4. **تصدير الملاحظات كـ PDF**
5. **مشاركة الاقتباسات على وسائل التواصل**
6. **تطبيق موبايل (PWA)**

---

## 🆘 الدعم

إذا واجهت مشكلة في إضافة كتاب، تحقق من:
- ✅ صحة مسارات الملفات في books.json
- ✅ ترميز UTF-8 لكل الملفات
- ✅ صحة بنية JSON (استخدم jsonlint.com)
- ✅ الفصول موجودة فعلياً في المجلد

**كيفية الاستخدام الآن:**
1. أضف ملفات HTML لكتبك في `/grammar/data/books/`
2. حدّث `books.json`
3. انشر بـ `DEPLOY.bat`
4. جاهز! 🎉
